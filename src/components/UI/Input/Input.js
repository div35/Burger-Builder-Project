import React from "react";

const input = (props) => {
    
    return (
        <div>
            <label>{props.label}</label>
            <input></input>
        </div>
    );

}

export default input;